<div class="w-full max-w-6xl p-6 flex justify-center">
  <div id="<%= dom_id(content) %>" class="border border-gray-200 p-4 rounded-lg shadow-md hover:shadow-lg transition-shadow duration-300">
    <h2 class="font-semibold text-indigo-700 text-lg mb-2"><%= content.title %></h2>
    <p class="text-gray-600 mb-4"><%= content %></p>
    <%= link_to "View Content", edit_admin_content_path(content), class: "text-blue-500 hover:underline" %>
  </div>
</div>
