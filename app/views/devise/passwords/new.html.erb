<div class="flex">
  <!-- Image Section -->
  <div class="w-1/2 mt-12 ml-20 mr-10 bg-cover bg-center rounded-lg" style="background-image: url('<%= asset_path('bglogin.jpeg') %>');">
  </div>

  <!-- Form Section -->
  <div class="w-1/2 max-w-md p-8 rounded-lg ml-auto mr-16">
    <h2 class="text-3xl font-bold text-center mb-6" style="color: #23A50A;">Lupa Password</h2>

    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: 'space-y-6' }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="space-y-4">
        <div>
          <%= f.label :email, "Email", class: 'block text-gray-700 text-sm font-semibold mb-2' %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-green-500' %>
        </div>
      </div>

      <div class="flex items-center">
        <%= f.submit "Send me reset password instructions", class: 'px-4 py-2 text-white font-semibold rounded-lg', style: 'background-color: #23A50A; '%>
      </div>
    <% end %>

    <div class="mt-6 text-center">
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
